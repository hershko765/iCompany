<script type="text/html">
    <div class="datagrid-container" style="position: relative;">
    <table class="table table-striped table-bordered table-hover">
        <thead class="thin-border-bottom">
        <tr class="datagrid-thead">
            <@ if(checkedColumn) { @>
                <th>
                    <label style="margin-left: 24px; margin-top: -7px" >
                        <input type="checkbox" class="ace datagrid-checkbox datagrid-checkable-checkall"  />
                        <span class="lbl"></span>
                    </label>
                </th>
            <@ } @>
            <@ _.each(headers,function(header){ @>
            <th class="sort-header" data-sort="{{ header.name }}">{{ header.title }}</th>
            <@ }) @>
            <th>Action</th>
        </tr>
        </thead>
        <tbody></tbody>
    </table>
    </div>
</script>